services:
  - type: web
    name: exam-prep
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --host=0.0.0.0 --port=$PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false
